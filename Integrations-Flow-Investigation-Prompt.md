# CLAUDE CODE — INTEGRATIONS CONNECT FLOW INVESTIGATION
> Generated 2026-05-07
> Read-only investigation. No code changes. Single deliverable: a short report.

---

## What this is

We've established that OAuth is failing because the connect flow on `/integrations` is firing without a `business_id`, producing a NULL `business_id` in the integrations table that breaks the upsert. The existing PK integration works correctly (rows with valid business_ids), so the fix is to make the Fortnox connect flow follow whatever pattern PK uses.

This task answers three questions so we can write the fix correctly:

1. How is the `/integrations` page currently structured — per-business cards or per-provider cards?
2. How does the PK connect flow get a valid `business_id` into the OAuth state?
3. How does the Fortnox connect flow currently get (or fail to get) a `business_id`?

The goal is to understand the existing pattern so the Fortnox fix can mirror it rather than invent a parallel approach.

---

## Hard constraints

- **Read-only.** Don't modify any files. Don't run migrations. Don't initiate any OAuth flows.
- **Don't write the fix yet.** This is investigation. The fix happens after we read the report.
- **Don't propose alternative architectures.** Per-business is decided. Just describe what exists and what the gap is.
- **No FIXES.md entry.** Nothing is being fixed in this task.

---

## What to investigate

### 1. The /integrations page structure

Read `app/integrations/page.tsx` (per the audit, it's 28k lines). Look specifically at:

- How is the page laid out? Are there per-business sections? Per-provider sections? A grid of cards? Tabs?
- Where is the list of the user's businesses fetched and displayed?
- For each business displayed, what integrations are shown — and how is each integration's state (connected/disconnected) determined?
- Is there a `connectFortnox(businessId)` function? Is there a similar `connectPK(businessId)` or equivalent? What's the call site for each?

Describe the page's structure in plain language. A few sentences plus 2-3 representative code snippets.

### 2. The PK connect flow

Walk through what happens when a user clicks "Connect Personalkollen" (or whatever the PK connect button says). Specifically:

- What handler runs on click?
- How does the handler determine which business this PK connection is for?
- Where does the `business_id` come from — a prop, state, URL parameter, the user's current selection?
- How does that `business_id` get passed into the OAuth/credentials flow?

The PK integration in the database has correct `business_id` values, so this flow works. We need to understand HOW it works so we can copy the pattern.

### 3. The Fortnox connect flow's current behaviour

Walk through what happens when a user clicks "Connect Fortnox":

- Where is `connectFortnox(businessId)` called from?
- What value is being passed as `businessId`? Is it actually getting a real business_id, or is it being called with undefined / empty / a placeholder?
- Why might it be passing empty when PK passes a real value? Is the call site different? Is the state management different?

The OAuth state we saw has `businessId: ""`, and Claude Code's earlier diagnostic noted the code converts `''` to `null`. Find the upstream cause: where in the flow does the empty value originate?

### 4. The gap, named

In one paragraph: what specifically does the Fortnox connect flow do differently from the PK connect flow that produces an empty `business_id`?

Possible root causes (don't speculate beyond what the code shows):
- Different call sites with different inputs
- Different state management
- A bug specifically in the Fortnox path that doesn't exist in the PK path
- A page-level issue where Fortnox is rendered outside per-business context while PK is rendered inside

Whatever it is, name it specifically with code references.

---

## Deliverable

A single markdown file at repo root: `INTEGRATIONS-FLOW-INVESTIGATION-2026-05-07.md`. Don't commit it; Paul reads and decides next steps.

Structure:

```markdown
# /integrations Connect Flow Investigation
> Generated by Claude Code on [date]
> Read-only. Comparison of PK (working) vs Fortnox (broken) connect flows.

## 1. Page structure
[2-4 paragraphs describing how /integrations is laid out, with line references]

## 2. PK connect flow (working pattern)
[Step-by-step: click → handler → business_id resolution → OAuth/credentials. With code excerpts.]

## 3. Fortnox connect flow (broken pattern)
[Same structure as PK section. Identify where business_id is lost or not passed.]

## 4. The gap
[One paragraph naming exactly what's different and where.]

## 5. Recommended fix shape
[2-3 sentences only. What change to which file would align Fortnox's behaviour with PK's. Don't write code; describe the change at a high level.]
```

End with this exact line:

> "Investigation complete. No code changed. Ready for review."

Then stop. Don't propose code. Don't open PRs.

---

## Time budget

30-45 minutes. This is a focused read of two specific code paths and a comparison. If you're past an hour, surface what's hard before continuing.

---

## What success looks like

After Paul reads the report:

1. He understands why PK works and Fortnox doesn't
2. He knows exactly which file(s) and which lines need to change
3. He can write a focused, small follow-up prompt to make the fix in 30 minutes

If the report makes Paul feel like the fix is complicated, surface what's hard. The expected fix is small (probably a few lines in `app/integrations/page.tsx`); if it looks bigger, that's information worth knowing.
