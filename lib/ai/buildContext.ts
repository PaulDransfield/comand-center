// lib/ai/buildContext.ts
//
// DEPRECATED — superseded by lib/ai/contextBuilder.ts (2026-04-23). This
// file used to export `buildLiveContext(orgId)` but was never imported
// anywhere. The replacement module centralises the /api/ask context-
// assembly logic that used to live inline in the ask route.
//
// Kept as a breadcrumb so anyone grepping the old name finds the move.
// Safe to delete once no branches reference it.

export {}
